feat: Implement role-based access control for Admin and Manager

Backend Changes:
- Update RolesGuard to use case-insensitive role comparison
- Add @Roles('Admin', 'Manager') to TreesController endpoints (create, update, delete, health)
- Add @Roles('Admin', 'Manager') to MaintenanceController endpoints (create, update status, stats, export)
- Keep existing @Roles('Admin') for AuthController user management endpoints
- Keep existing admin check in AuditLogController

Frontend Changes:
- AppShell.tsx: Navigation menu already configured correctly with role-based filtering
- App.tsx: Route guards already protecting routes properly
- RoleGuard.tsx: Already using case-insensitive role comparison
- Fix syntax error in types/index.ts (missing closing brace for PaginatedActivityLogs)

Role Permissions:
- Admin: Full system access (user management, audit logs, all manager features)
- Manager: Operational management (dashboard, trees, tasks, staff stats)
- Staff: Field operations only (map view, assigned tasks)

Security:
- Two-layer protection: Frontend (RoleGuard) + Backend (@Roles decorator)
- Case-insensitive role comparison to prevent case-sensitivity issues
- Manager cannot access user management or audit logs
- Admin has all permissions including manager features

Build Status:
✅ Backend build successful
✅ Frontend build successful
✅ No TypeScript errors
✅ No diagnostics issues
